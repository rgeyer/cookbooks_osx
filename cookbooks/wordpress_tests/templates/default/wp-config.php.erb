<?php
define('DB_NAME', '<%= node[:wordpress_tests][:dbname] %>');    // The name of the database
define('DB_USER', '<%= node[:wordpress_tests][:dbuser] %>');     // Your MySQL username
define('DB_PASSWORD', '<%= node[:wordpress_tests][:dbpass] %>'); // ...and password
define('DB_HOST', '<%= node[:wordpress_tests][:dbhost] %>');    // 99% chance you won't need to change this value
define('DB_CHARSET', '<%= node[:wordpress_tests][:dbcharset] %>');
define('DB_COLLATE', '<%= node[:wordpress_tests][:dbcollate] %>');

// You can have multiple installations in one database if you give each a unique prefix
$table_prefix  = 'wp_';   // Only numbers, letters, and underscores please!

// Change this to localize WordPress.  A corresponding MO file for the
// chosen language must be installed to wp-content/languages.
// For example, install de.mo to wp-content/languages and set WPLANG to 'de'
// to enable German language support.
define ('WPLANG', '');

// uncomment and change this if you'd like to load plugins from a particular directory prior to testing
define('DIR_TESTPLUGINS', '<%= node[:wordpress_tests][:project_path] %>');
define('DIR_TESTDATA', '<%= ::File.join(node[:wordpress_tests][:project_path], "wordpress-tests", "wp-testdata") %>');

// Overwrite the ABSPATH for testing purposes, this is so that all of your include & require statements work in the test environment
define('ABSPATH', '<%= ::File.join(node[:wordpress_tests][:project_path], "wordpress-tests", "wordpress") %>/');
?>
